package utils;

/**
 * This enum contains enum constants used for associating every entity the corresponding type.
 * @author Teodor-Adrian Mirea, 323CA
 */
public enum EntityType {
    FILE,
    DIRECTORY
}
